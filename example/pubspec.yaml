name: example
description: DuskMoon UI showcase app
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  duskmoon_ui:
    path: ../packages/duskmoon_ui
  duskmoon_theme_bloc:
    path: ../packages/duskmoon_theme_bloc
  flutter_bloc: ^9.0.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

## 0.1.0

- Initial release
- Flutter `RenderBox` Mermaid renderer with direct Canvas painting
- Basic flowchart parser, IR, rank layout, and scene model
- Common node shapes, edge styles, arrowheads, and edge labels
- Markdown integration through `DmMarkdown` and `DmMarkdownInput`
